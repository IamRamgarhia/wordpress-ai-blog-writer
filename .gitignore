vendor/
node_modules/
.phpunit.result.cache
*.log
.superpowers/

# Build artifacts. Rebuilt by bin/build.py from whatever is committed,
# so a copy in git is only ever a stale duplicate of the source beside it.
*.zip

# Editor/tool scratch, not part of the plugin.
.zcode/

# Scraping artefacts, never part of the plugin.
rr.json
sg.html
junit.xml
phpunit-out.txt
